#include<stdio.h>
int main()
{
    int a;
    char c;
    float f;
    double d;  
    printf("The size of int var is %d",sizeof(a));
     printf("\nThe size of char var is %d",sizeof(c));
      printf("\nThe size of float var is %d",sizeof(f));
       printf("\nThe size of double var is %d",sizeof(d));
    return 0;
}
